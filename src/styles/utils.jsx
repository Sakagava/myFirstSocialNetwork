import styled from '@emotion/styled'
import { Box } from '@mui/material'

export const CenteringWrap = styled(Box)(() => ({
	display: 'flex',
	flexDirection: 'column',
	alignItems: 'center',
	justifyContent: 'center',
}))

export const CenteringWrapCSS = {
	display: 'flex',
	flexDirection: 'column',
	alignItems: 'center',
	justifyContent: 'center',
}
